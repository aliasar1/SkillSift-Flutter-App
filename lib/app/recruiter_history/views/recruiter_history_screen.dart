import 'package:flutter/material.dart';

class RecruiterHistoryScreen extends StatelessWidget {
  const RecruiterHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
