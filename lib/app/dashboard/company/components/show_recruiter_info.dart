import 'package:flutter/material.dart';

class ShowRecruiterInfo extends StatelessWidget {
  const ShowRecruiterInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold());
  }
}
